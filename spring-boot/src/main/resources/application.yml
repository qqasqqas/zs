#DB
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&characterEncoding=utf8&serverTimezone=CTT
    username: root
    password: root
mybatis:
  #pojo别名扫描包
  type-aliases-package: com.answer.pojo
  #加载Mybatis映射文件
  mapper-locations: classpath:mapper/*Mapper.xml